# Production Environment Variables
# Copy this file to .env and update the values

# Redis Configuration
# Generate a secure password: openssl rand -base64 32
REDIS_PASSWORD=your_secure_redis_password_here

# CORS Configuration
# Add your production domain
CORS_ALLOWED_ORIGINS=https://yourdomain.com,http://yourdomain.com
WEBSOCKET_ALLOWED_ORIGINS=https://yourdomain.com,http://yourdomain.com

# Frontend URLs (configured via Nginx reverse proxy)
VITE_API_URL=https://yourdomain.com/api
VITE_WS_URL=https://yourdomain.com/ws

# Optional: Uncomment and configure if needed
# VITE_ENV=production
